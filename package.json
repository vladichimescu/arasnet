{
  "name": "arasnet",
  "version": "0.1.0",
  "workspaces": {
    "packages": [
      "portal",
      "server",
      "libs/*"
    ]
  },
  "private": true,
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "clean": "rm -rf .yarn portal/build && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "format": "prettier --write \"**/*.{css,scss,js,jsx,json,md}\"",
    "start": "yarn workspaces foreach --all --parallel run start",
    "build": "yarn workspaces foreach --all --parallel run build",
    "serve": "yarn workspaces foreach --all --parallel run serve"
  },
  "engines": {
    "node": "20.16.0",
    "yarn": "4.3.1"
  },
  "volta": {
    "node": "20.16.0",
    "yarn": "4.3.1"
  },
  "prettier": {
    "semi": false,
    "trailingComma": "es5",
    "plugins": [
      "@trivago/prettier-plugin-sort-imports"
    ],
    "importOrder": [
      "^@arasnet/(.*)$",
      "^../",
      "^./",
      "^./*.*.(c|sc)ss"
    ],
    "importOrderSeparation": true,
    "importOrderSortSpecifiers": true
  }
}
